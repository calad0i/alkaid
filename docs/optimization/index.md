# Optimization

Alkaid applies intensive optimizations when converting a dataflow graph to hardware. The optimizations are applied at different stages of the compilation process, including both symbolic tracing and post-tracing graph rewrites. The following pages describe the optimizations applied.

```{toctree}
:maxdepth: 1

affine_interval
scm
cmvm
graph_passes
```
